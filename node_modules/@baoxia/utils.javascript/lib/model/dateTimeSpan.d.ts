export declare class DateTimeSpan {
    years: number;
    months: number;
    days: number;
    hours: number;
    minutes: number;
    seconds: number;
    milliseconds: number;
    get totalYears(): number;
    set totalYears(years: number);
    get totalMonths(): number;
    set totalMonths(months: number);
    get totalDays(): number;
    set totalDays(days: number);
    get totalHours(): number;
    set totalHours(hours: number);
    get totalMinutes(): number;
    set totalMinutes(minutes: number);
    get totalSeconds(): number;
    set totalSeconds(seconds: number);
    get totalMilliseconds(): number;
    set totalMilliseconds(milliseconds: number);
    constructor(milliseconds?: number, seconds?: number, minutes?: number, hours?: number, days?: number, months?: number, years?: number);
}
//# sourceMappingURL=dateTimeSpan.d.ts.map