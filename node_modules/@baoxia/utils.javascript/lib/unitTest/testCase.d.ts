export declare class TestCase {
    name: string;
    testRunable: (assert: (isTure: boolean) => any, assertFalse: (isTure: boolean) => any) => any;
    /**
     * 断言指定的测试结果为“true”。
     * @param isTure 指定的测试结果。
     */
    static assertTrue(isTure: boolean): void;
    /**
     * 断言指定的测试结果为“false”。
     * @param isFalse 指定的测试结果。
     */
    static assertFalse(isFalse: boolean): void;
    /**
     * 断言指定的测试结果为“true”。
     * @param isTure 指定的测试结果。
     */
    static assert(isTure: boolean): any;
    /**
     * 构建测试用例基本信息。
     * @param name 当前测试用例的名称。
     * @param runable 当前测试用例的测试内容。
     */
    constructor(name: string, testRunable: (assert: (isTure: boolean) => any, assertFalse: (isTure: boolean) => any) => any);
    test(): any | null;
}
//# sourceMappingURL=testCase.d.ts.map