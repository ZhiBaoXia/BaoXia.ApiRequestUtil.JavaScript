import { NumberRoundType } from "./constant/numberRoundType.js";
export declare class NumberUtil {
    /**
     * 在指定的数值范围内，生成一个随机整数。
     * @param min 指定数值范围的最小值。
     * @param max 指定数值范围的最大值。
     * @returns 生成的随机整数。
     */
    static randomInt(min: number, max: number): number;
    /**
     * 在指定的数值范围内，生成一个随机浮点数。
     * @param min 指定数值范围的最小值。
     * @param max 指定数值范围的最大值。
     * @returns 生成的随机浮点数。
     */
    static randomFloat(min: number, max: number): number;
    /**
     * 根据指定的小数精度（位数），对指定的数值进行取整。
     * @param numberValue 指定的数值。
     * @param floatPrecision 指定的小数精度（位数）。
     * @param numberRoundType 指定的取整类型，默认为：NumberRoundType.Round，四舍五入。
     * @returns 取整后的数值。
     */
    static numberByFixed(numberValue: number, floatPrecision: number, numberRoundType?: NumberRoundType): number;
}
//# sourceMappingURL=numberUtil.d.ts.map