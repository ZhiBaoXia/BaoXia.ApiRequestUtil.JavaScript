import { TestCase } from "@baoxia/utils.javascript";
export class PostRequestTest extends TestCase {
    ////////////////////////////////////////////////
    // @自身实现
    ////////////////////////////////////////////////
    constructor() {
        super("PostRequest Test", (assert, assertFalse) => {
        });
    }
}
